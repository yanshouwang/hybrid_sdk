// Autogenerated from Pigeon (v22.4.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon

#undef _HAS_EXCEPTIONS

#include "hybrid_v4l2_api.h"

#include <flutter/basic_message_channel.h>
#include <flutter/binary_messenger.h>
#include <flutter/encodable_value.h>
#include <flutter/standard_message_codec.h>

#include <map>
#include <optional>
#include <string>

using flutter::BasicMessageChannel;
using flutter::CustomEncodableValue;
using flutter::EncodableList;
using flutter::EncodableMap;
using flutter::EncodableValue;

FlutterError CreateConnectionError(const std::string channel_name) {
  return FlutterError(
      "channel-error",
      "Unable to establish connection on channel: '" + channel_name + "'.",
      EncodableValue(""));
}


PigeonInternalCodecSerializer::PigeonInternalCodecSerializer() {}

EncodableValue PigeonInternalCodecSerializer::ReadValueOfType(
  uint8_t type,
  flutter::ByteStreamReader* stream) const {
  return flutter::StandardCodecSerializer::ReadValueOfType(type, stream);
}

void PigeonInternalCodecSerializer::WriteValue(
  const EncodableValue& value,
  flutter::ByteStreamWriter* stream) const {
  flutter::StandardCodecSerializer::WriteValue(value, stream);
}

/// The codec used by ViewHostAPI.
const flutter::StandardMessageCodec& ViewHostAPI::GetCodec() {
  return flutter::StandardMessageCodec::GetInstance(&PigeonInternalCodecSerializer::GetInstance());
}

// Sets up an instance of `ViewHostAPI` to handle messages through the `binary_messenger`.
void ViewHostAPI::SetUp(
  flutter::BinaryMessenger* binary_messenger,
  ViewHostAPI* api) {
  ViewHostAPI::SetUp(binary_messenger, api, "");
}

void ViewHostAPI::SetUp(
  flutter::BinaryMessenger* binary_messenger,
  ViewHostAPI* api,
  const std::string& message_channel_suffix) {
  const std::string prepended_suffix = message_channel_suffix.length() > 0 ? std::string(".") + message_channel_suffix : "";
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.hybrid_v4l2.ViewHostAPI.registerTexture" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          ErrorOr<int64_t> output = api->RegisterTexture();
          if (output.has_error()) {
            reply(WrapError(output.error()));
            return;
          }
          EncodableList wrapped;
          wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
          reply(EncodableValue(std::move(wrapped)));
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.hybrid_v4l2.ViewHostAPI.updateTexture" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_id_args_arg = args.at(0);
          if (encodable_id_args_arg.IsNull()) {
            reply(WrapError("id_args_arg unexpectedly null."));
            return;
          }
          const int64_t id_args_arg = encodable_id_args_arg.LongValue();
          const auto& encodable_buffer_args_arg = args.at(1);
          if (encodable_buffer_args_arg.IsNull()) {
            reply(WrapError("buffer_args_arg unexpectedly null."));
            return;
          }
          const auto& buffer_args_arg = std::get<std::vector<uint8_t>>(encodable_buffer_args_arg);
          const auto& encodable_width_args_arg = args.at(2);
          if (encodable_width_args_arg.IsNull()) {
            reply(WrapError("width_args_arg unexpectedly null."));
            return;
          }
          const int64_t width_args_arg = encodable_width_args_arg.LongValue();
          const auto& encodable_height_args_arg = args.at(3);
          if (encodable_height_args_arg.IsNull()) {
            reply(WrapError("height_args_arg unexpectedly null."));
            return;
          }
          const int64_t height_args_arg = encodable_height_args_arg.LongValue();
          std::optional<FlutterError> output = api->UpdateTexture(id_args_arg, buffer_args_arg, width_args_arg, height_args_arg);
          if (output.has_value()) {
            reply(WrapError(output.value()));
            return;
          }
          EncodableList wrapped;
          wrapped.push_back(EncodableValue());
          reply(EncodableValue(std::move(wrapped)));
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.hybrid_v4l2.ViewHostAPI.unregisterTexture" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_id_args_arg = args.at(0);
          if (encodable_id_args_arg.IsNull()) {
            reply(WrapError("id_args_arg unexpectedly null."));
            return;
          }
          const int64_t id_args_arg = encodable_id_args_arg.LongValue();
          std::optional<FlutterError> output = api->UnregisterTexture(id_args_arg);
          if (output.has_value()) {
            reply(WrapError(output.value()));
            return;
          }
          EncodableList wrapped;
          wrapped.push_back(EncodableValue());
          reply(EncodableValue(std::move(wrapped)));
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
}

EncodableValue ViewHostAPI::WrapError(std::string_view error_message) {
  return EncodableValue(EncodableList{
    EncodableValue(std::string(error_message)),
    EncodableValue("Error"),
    EncodableValue()
  });
}

EncodableValue ViewHostAPI::WrapError(const FlutterError& error) {
  return EncodableValue(EncodableList{
    EncodableValue(error.code()),
    EncodableValue(error.message()),
    error.details()
  });
}

